<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.programmers.fyp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Specialization_District" />
        <activity android:name=".Doctor_Profile" />
        <activity android:name=".TimeSelect" />
        <activity android:name=".UserInfo" />
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".LoginORbook" />
        <activity android:name=".Adminpanel" />
        <activity android:name=".UserandPass"></activity>
    </application>

</manifest>